'''
We can run modified greedy BFS on the current location the user is at using the dist the user input.



for all v in V
    visited[v] <- 0
    p[v] <- -1

BFS recursive(dist, [selected edges]):

    for all v adjacent to u:

'''